class Main extends IO{
  main() : int32{
    let x : int32 <- 7 in
    x;
    let y : int32 <- 8 in
    let x : bool <- true in
    printBool(x).printInt32(y).print("\n");
    printBool(x).printInt32(y).print("\n");
    10
  }

  other() : int32{
    7
  }

class Main {
  io : IO <- new IO;
  f() : int32 {
    io.print("caca", );
    0
  };
}
